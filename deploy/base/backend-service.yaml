apiVersion: v1
kind: Service
metadata:
  name: hashpost-backend
  namespace: hashpost
spec:
  selector:
    app: hashpost-backend
  ports:
  - port: 8888
    targetPort: 8888
    protocol: TCP
  type: ClusterIP
