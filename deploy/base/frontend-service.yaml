apiVersion: v1
kind: Service
metadata:
  name: hashpost-frontend
  namespace: hashpost
spec:
  selector:
    app: hashpost-frontend
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
